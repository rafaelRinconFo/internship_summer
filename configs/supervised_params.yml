dataset_path: /home/rafa/Documents/internship_summer/datasets/2020
model_type: MiDaS_small # It can be DPT_Large, DPT_Hybrid or MiDaS_small
train_size: 0.8
val_size: 0.1
batch_size: 8
epochs: 15
lr: 0.0001
weight_decay: 0.000001
loss: Huber # It can be L1, MSE, Huber, SIL, SSIM is still WIP
log_metrics_every: 5 # Log metrics every n epochs
desired_metrics: ["accuracy_threshold", "RMSE", "RMSLE"]